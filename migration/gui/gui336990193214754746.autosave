WINDOW_WIDTH = 800
WINDOW_HEIGHT = 800

a = 0

def setup(): 
  size(WINDOW_WIDTH, WINDOW_HEIGHT)
  background(0)
  noStroke()
  fill(102)

def draw():
  background(0)
  global a
  a = (a + 1) % width
  rect(a, 10, 2, 80)
